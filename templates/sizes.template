// Constants
{{range .Constants}}
const {{cpptype .Typename}} {{.Name}} = {{.Value}};
{{end}}

// Arrays
{{range .ArrayTypes}}
{{end}}

// Structs
{{range .StructTypes}}
struct {{.Name}} // size {{.Size}}
{
{{- range .Fields}}
    {{cpptype .Typename}}	{{.Name}}; // @ {{.Offset}}, {{.Size}} bytes
{{- end}}
};
{{end}}
